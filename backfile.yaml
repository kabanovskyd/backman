# authentication file location
authentication_file: ./auth_file.json

google_sheet:
  sheet_url:
  sheet_credentials:

cron:
  tracking:
  user:
  schedule:

# directories list
directories:
  /data/slidetag:
    bucket: sodium_archive_manual
    active: true
    subdirs:
      - "daniel_new"